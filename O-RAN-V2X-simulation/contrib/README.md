# External ns-3 modules

The O-RAN interface is maintained separately and is not vendored in this
repository. Install it here before configuring ns-3:

```bash
git clone -b master https://github.com/o-ran-sc/sim-ns3-o-ran-e2 oran-interface
```

The resulting local path must be `contrib/oran-interface`.
